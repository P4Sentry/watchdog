version: "3"
services:
    node01:
        build: .
        image: watchdog
        environment:
            - PORT_NUM=8
            - GRPC_PORT=9559
        ports:
            - "9559:9559"
        cap_add:
            - NET_ADMIN
    node02:
        image: watchdog
        environment:
            - PORT_NUM=8
            - GRPC_PORT=9559
        ports:
            - "9569:9559"
        cap_add:
            - NET_ADMIN
